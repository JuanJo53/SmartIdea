import { CountRespCorrectasPipe } from './count-resp-correctas.pipe';

describe('CountRespCorrectasPipe', () => {
  it('create an instance', () => {
    const pipe = new CountRespCorrectasPipe();
    expect(pipe).toBeTruthy();
  });
});
