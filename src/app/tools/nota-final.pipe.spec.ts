import { NotaFinalPipe } from './nota-final.pipe';

describe('NotaFinalPipe', () => {
  it('create an instance', () => {
    const pipe = new NotaFinalPipe();
    expect(pipe).toBeTruthy();
  });
});
