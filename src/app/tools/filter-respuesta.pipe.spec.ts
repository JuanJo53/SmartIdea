import { FilterRespuestaPipe } from './filter-respuesta.pipe';

describe('FilterRespuestaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRespuestaPipe();
    expect(pipe).toBeTruthy();
  });
});
